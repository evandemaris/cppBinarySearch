//Algorithm for performing a binary search through a sorted array.
//Binary search should allow significantly lower seek times to find data.

int BinarySearch(int myArray[], int arrayLength, int target) {
	return 0;
}